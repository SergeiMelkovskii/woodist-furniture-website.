# Admin password for /admin login (plain text; change to a strong value)
ADMIN_PASSWORD=change-me-now

# 32+ character secret for signing admin session cookies
SESSION_SECRET=replace-with-a-long-random-string-at-least-32-chars-long
